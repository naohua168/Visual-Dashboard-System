# Visual Dashboard System — Python 依赖
# 安装: pip install -r requirements.txt

# 数据处理
pandas>=1.5.0
openpyxl>=3.1.0          # 读取 .xlsx
xlrd>=2.0.0               # 读取 .xls

# HTML 渲染
jinja2>=3.1.0             # 模板引擎

# 测试（开发依赖）
pytest>=7.0.0
