feat: meta 按页动态显示数据起止 + 生成时间含年月日

每页 render() 计算自己的 date_range 并注入 page 元素的
data-range 属性。

utils.py 新增 extract_date_range(df, col) 工具函数。

base.py 的 wrap_page 接收可选 date_range 参数。

每页用不同数据源反映真实页面数据:
- overview: data.income
- annual: data.income (年度累计)
- yoy: data.income + yearly baseline
- quarterly: data.quarterly_income
- monthly: data.income (月累计可换 monthly_income_detail)
- sales: data.sales_income

run.py:
- 生成时间格式: %H:%M -> %Y-%m-%d %H:%M (含年月日)
- meta 加 id="pageMeta" data-default="..." 属性供 JS 回退

GLOBAL_JS:
- showPage(id) 切换时根据 pageEl.data-range 更新 meta
- 首屏初始化也按第一页 data-range 设置 meta