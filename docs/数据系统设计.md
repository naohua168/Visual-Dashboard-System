# 数据系统设计

> 版本：v6.1 | 更新：2026-08-25
> 涵盖：系统结构 / 原始数据 / 清洗流水线 / 输出格式 / 配置驱动 / 测试
> **字段↔列名对照**已拆分为独立文档：[字段映射.md](字段映射.md)

---

## 一、系统结构

```
Visual Dashboard_system/
├── main.py                           # ★ 顶层调度器（argparse + subprocess）
├── run_all.bat                       # ★ Windows 一键运行（配置同步 + 全流程 + 看板验证）
├── package.bat                       #   打包脚本（生成交付 zip）
├── config/
│   ├── 配置编辑器.xlsx               # ★ 配置编辑层（时间配置/展示规则/KPI指标/销售归属/说明 5个sheet）
│   ├── 清洗配置/
│   │   ├── cleaning_config.json      # ★ 中央清洗配置（数据源/列映射/时间/输出）
│   │   └── 客户销售归属.json          #   母公司→子公司→销售分配规则
│   └── 前端渲染/
│       ├── 看板展示配置.json          #   页面顺序/标题/输出命名
│       └── 展示规则.json              #   筛选/排序/TopN/优先展示
├── data/
│   ├── raw/                          # 【用户维护】原始 Excel（只读）
│   ├── mappings/                     # 【系统生成+用户维护】映射 JSON
│   └── sheets/
│       ├── 手动维护/                 # 【用户维护】6张指标表
│       └── 系统数据清理/             # 【系统生成】清洗输出（10个目录）
├── engine/                           # ★ 数据清洗引擎
│   ├── core/                         #   config / column_resolver / customer_matcher / mapping_loader / utils
│   ├── income_payment/               #   Phase 1+2: financial.py + operations.py + run.py
│   ├── sales/                        #   Phase 3: run.py
│   └── yearly_baseline/              #   Phase 0: cleaner.py + run.py
├── processors/                       # ★ 渲染层（6 页看板）
│   ├── run.py                        #   主入口（合并6页 → 单个HTML + Excel总表）
│   ├── data_loader.py                #   数据加载（CustomerUnifier 统称归并）
│   ├── page_data.py                  #   数据层（6 dataclass + 6 prepare 函数）
│   ├── page_data_utils.py            #   共享数据工具（聚合/排序/筛选/子公司明细）
│   ├── page_overview.py              #   数据总览
│   ├── page_annual.py                #   年度达成
│   ├── page_monthly.py               #   月度达成
│   ├── page_quarterly.py             #   季度达成
│   ├── page_sales.py                 #   销售达成（3卡片 + 右侧抽屉弹窗）
│   ├── page_yoy.py                   #   年度同比
│   ├── sales_pending.py              #   待确认客户弹窗
│   ├── components.py                 #   共享组件（弹窗/部门卡/矩阵cell）
│   ├── config_loader.py              #   前端配置加载
│   ├── base.py / hero.py             #   全局 CSS / Hero 组件
│   ├── utils.py                      #   fmt_wan / fmt_pct / fmt_yoy / safe_float
│   └── static/                       #   CSS/JS/图标/Chart.js（含 icons.py、chart.umd.min.js）
├── scripts/                          # ★ 工具脚本
│   ├── config_excel_to_json.py       #   配置编辑器 Excel → JSON 同步器
│   └── verify_dashboard.py           #   看板质量验证（JS语法/onclick函数）
├── tests/                            # 16 文件，147 passed（含 conftest.py）
├── docs/                             # 数据系统设计 / 字段映射 / 部署指南 / 维护指南
├── output/                           # ★ 看板输出（不上传 Git）
│   ├── 看板/看板_YYYYMMDD.html        #   6 页可视化看板
│   └── 数据/data_YYYYMMDD.xlsx        #   16 Sheet 数据总表（sheet名=data/sheets/文件夹名）
└── logs/                             # 运行日志
```

---

## 二、原始数据（`data/raw/`）

### 2.1 数据源全景

| 数据源 | 文件 | 时间口径 | 金额单位 | 引擎 |
|--------|------|---------|---------|------|
| 财务端收入 | `财务端数据/收入.xlsx` | **当月**单月 | 元 | openpyxl |
| 财务端回款 | `财务端数据/回款.xlsx` | **当月**单月 | 元 | openpyxl |
| 广东公司 | `财务端数据/广东公司.xlsx` | 累计简表 | **万元** | openpyxl |
| 湖南公司 | `财务端数据/湖南公司.xlsx` | 累计简表 | **万元** | openpyxl |
| 南方韶关 | `财务端数据/南方韶关.xlsx` | 累计简表 | **万元** | openpyxl |
| 运营端收入 | `运营端数据/收入.xls` | 当年累计（含"1-N月"） | 元 | xlrd |
| 运营端回款 | `运营端数据/回款.xls` | 当年累计（含"1-N月"） | 元 | xlrd |
| 往年收入 | `往年收入数据/往年年收入.xlsx` | 去年基线 | 元 | openpyxl |
| 往年回款 | `往年回款数据/往年年回款.xlsx` | 去年基线 | 元 | openpyxl |
| 客户名单 | `客户名单/客户名单.xlsx` | 白名单 471 家 | — | openpyxl |

> **字段↔列名映射**（候选列名优先级/当前命中列）见独立文档 [docs/字段映射.md](字段映射.md)。

> **单位换算**：清洗引擎统一输出"**元**"；广东/湖南（万元）自动 `×金额除数(10000)` 转元；渲染层再 `÷10000` 转"万元"展示。

### 2.2 财务端数据源（`cleaning_config.json → 数据源.财务端`）

**5 个来源**：收入.xlsx、回款.xlsx、广东公司.xlsx、湖南公司.xlsx、南方韶关.xlsx。

| 来源 | 列映射 | 金额单位 | 部门固定 | 白名单 |
|------|--------|---------|---------|--------|
| 收入.xlsx | 日期/客户/金额/部门/法人主体（列名匹配详见 [字段映射](字段映射.md)） | 元 | 成本中心映射 | ✅ 过 |
| 回款.xlsx | 同上 | 元 | 成本中心映射 | ✅ 过 |
| 广东公司.xlsx | 日期/客户/金额 | 万元 ×10000 | **检测** | ✅ 过 |
| 湖南公司.xlsx | 日期/客户/金额 | 万元 ×10000 | **信息** | ✅ 过 |
| 南方韶关.xlsx | **无表头**，列位置：客户=0/日期=1/金额=2 | 万元 ×10000 | **检测** | ✅ 过 |

> ⚠ **口径**：收入金额取 `不含税金额`，回款金额取 `应收金额`——这是看板数据与财务口径一致的关键。
> **客户区域筛选已于 2026-08-24 移除**：不再按 `客户区域=三区` 过滤，财务端保留全部区域客户。

**各来源候选列名/当前命中列**：见 [docs/字段映射.md](字段映射.md)。

### 2.3 运营端数据源（`cleaning_config.json → 数据源.运营端`）

**2 个来源**：收入.xls、回款.xls（引擎 xlrd，Sheet1）。

| 来源 | 列映射字段 | 金额单位 |
|------|-----------|---------|
| 收入.xls | 日期/客户/金额/部门/内部交易/核算单位/法人主体 | 元 |
| 回款.xls | 同上 | 元 |

**日期解析**（`_parse_ops_date`）：

| 原文 | 结果 | 逻辑 |
|------|------|------|
| `"1-4月"` | `Timestamp(2026,4,1)` | 取末月（正确归属对应季度） |
| `"5月"` | `Timestamp(2026,5,1)` | 直接解析 |
| 具体日期 | Timestamp | 精确 |
| NaN | → fixed_date | 兜底 |

**广东/深圳识别**：按 `法人主体`/`核算单位` 列含"广东"/"深圳"关键字 → **不经白名单**；其余运营端数据同样**不经白名单**（运营端全量保留）。

### 2.4 手工指标表（`data/sheets/手动维护/`，6 张）

统一列结构（按部门拆分指标值，单位：**万元**）：

| 列名 | 类型 | 说明 | 示例 |
|------|------|------|------|
| 客户 | 文本 | 客户全称 | 比亚迪汽车工业有限公司 |
| 销售 | 文本 | 负责销售名 | 黄灿 |
| **检测** | 数字 | 检测事业部指标（万元） | 5000 |
| **信息** | 数字 | 信息智能事业部指标（万元） | 500 |
| **能源** | 数字 | 能源动力事业部指标（万元） | 0 |
| **海外** | 数字 | 海外事业部指标（万元） | 0 |

> ⚠ **重要**：同一客户有多行时（不同销售负责不同部门），系统按客户名自动**累加**各部门指标，无需手动合并。

| 目录 | 文件名 | 对应页面 |
|------|--------|----------|
| `年度收入总指标/` | 年度收入总指标.xlsx | 年度达成（收入） |
| `年度回款总指标/` | 年度回款总指标.xlsx | 年度达成（回款） |
| `季度收入指标/` | 季度收入指标.xlsx | 季度达成（收入） |
| `季度回款指标/` | 季度回款指标.xlsx | 季度达成（回款） |
| `月度收入指标/` | 月度收入指标.xlsx | 月度达成（收入） |
| `月度回款指标/` | 月度回款指标.xlsx | 月度达成（回款） |

---

## 三、4-Phase 清洗流水线

```
Phase 0: 年基线清洗  往年收入/回款 → 往年收入/回款（6列标准表）
Phase 1+2: 收入/回款 财务端(当月) + 运营端(当年累计) → 三路输出（月/季度累计/当年累计）
Phase 3: 销售拆分    4层匹配 + 总额校验 → 销售收入/回款（7列，含销售列）
Phase 4: 渲染看板    6页HTML + Excel总表 → output/
```

### 3.1 Phase 0: 年基线清洗

**入口**：`engine/yearly_baseline/cleaner.py::run_clean()`

**查找**：从 `cleaning_config.json → 年基线数据` 读取精确文件路径（不再 glob 通配）。

**清洗步骤**：
1. `pd.read_excel(file_path)` 读取
2. 客户列重命名：`客户名称/客户名/客户` → `客户`
3. 金额列重命名：`收入金额/到款金额/收入/到款/金额` → `金额`
4. **内部交易过滤**（2026-08-31 新增）：① 列值过滤——`是否属于内部交易/是否属于内部款项/内部交易/内部款项` = "是" 的行排除（与运营端一致）；② 排除名单过滤——客户在 `部门事业部映射.json` 的 `excluded_internal_companies`（广东/深圳/湖南 3 家）中则排除（与财务端/销售层一致）
5. 事业部映射：`map_income_dept`（全称→简称）
6. `pd.to_numeric(errors="coerce")` 金额转数值
7. 日期列动态发现 → `_parse_yearly_date`（统一赋 `YEAR_LABEL-01-01`，无月份粒度）
8. 补齐 `是否为广东公司=""`、`是否为深圳公司=""`
9. 标准化输出 → 6列

**输出**：`data/sheets/系统数据清理/往年收入/` + `往年回款/`

> **年基线月份粒度检测**：`has_yearly_baseline` 检测 `nunique()<=1` → 返回 False → 页面同比自动降级不显示。

### 3.2 Phase 1: 财务端清洗

**入口**：`engine/income_payment/financial.py::clean_financial()`

**四路来源**：

| 函数 | 来源 | 特殊处理 |
|------|------|---------|
| `clean_financial_main` | 收入.xlsx / 回款.xlsx | `filter_by_date` → 内部交易排除 → 部门映射 → **白名单过滤** |
| `clean_guangdong` | 广东公司.xlsx | 金额×10000 → 部门="检测" → `是否为广东公司="是"` |
| `clean_hunan` | 湖南公司.xlsx | 金额×10000 → 部门="信息" |
| `clean_south_shaoguan` | 南方韶关.xlsx | 无表头按列位置 → 金额×10000 → 部门="检测" → 白名单过滤 |

> 说明：`clean_financial_main` 已移除「客户区域=三区」筛选（2026-08-24），财务端保留全部区域客户。

**输出**：`pd.concat([主表, 广东, 湖南, 南方韶关])` = 财务端当月完整数据（6列标准表）

### 3.3 Phase 2: 运营端清洗

**入口**：`engine/income_payment/operations.py::clean_operations()`

**清洗步骤**：
1. `pd.read_excel(file_path, sheet_name, engine="xlrd")` 读取
2. `extract_columns()` 列名提取
3. 内部交易过滤：剔除 `是否属于内部交易 == "是"`
4. `map_income_dept()` 事业部映射
5. `_parse_ops_date` 日期解析（取末月）
6. 广东/深圳识别：`法人主体.str.contains("广东|深圳")`
7. **运营端全量通过白名单**（不筛选）
8. `standardize_output()` → 6列

### 3.4 三路合并输出

**入口**：`engine/income_payment/run.py::run_clean()`

```python
df_monthly   = df_financial                                   # 月数据（仅财务端当月）
df_yearly    = concat([df_financial, df_ops])                 # 当年累计（全量）
df_quarterly = concat([df_financial, _filter_ops_by_quarter(df_ops)])  # 季度累计
```

**季度筛选**：`_filter_ops_by_quarter()` → 按时间范围配置筛选运营端月份

| 输出 | 数据组成 | 用途 |
|------|---------|------|
| 月收入/月回款 | 仅财务端当月 | 月度达成页 |
| 当年累计收入/回款 | 财务端+运营端全量 | 年度达成+销售拆分 |
| 季度累计收入/回款 | 财务端+运营端当季 | 季度分析页 |

### 3.5 Phase 3: 销售拆分

**入口**：`engine/sales/run.py::run_split()`

**数据源**：`config/清洗配置/客户销售归属.json`（125 母公司 → 577 子公司 → 收入/回款 → 4部门 → 销售 → 比例）

**拆分逻辑**：
1. 客户名 → 匹配归属：查找 `母公司.子公司`，子公司名=客户名 或 客户名命中母公司归拢名
2. 命中 → 取该子公司对应指标（收入/回款）×部门 的销售分配
3. 比例默认 1.0（全额）；`0<比例≤1` 支持拆分（如 比亚迪 多销售按比例拆）
4. 未命中 → 标记**"待确认"**（不丢弃，用于待确认客户弹窗）

**科技公司销售拆分**（`展示规则.json → _销售拆分.客户矩阵`）：
- 矩阵行按 `母公司·销售` 拆分：`科技公司 → 科技公司·王海龙(34家) / 科技公司·李巍(98家)`
- 每行只统计该销售名下子公司的实际/目标，两销售相加 = 科技公司全部（132家）
- 年度指标表按"子公司名+销售"维护；月度/季度指标表按"客户=科技公司+销售"维护，`_consolidate_target` 两种口径均处理
- 拆分键的弹窗"本部"目标只取该销售（`target_by_sales`），不混入对方

**比亚迪法人主体拆分**（`_apply_byd_sales_override`）：
- 扫描原始数据找出重名子公司 → 按法人主体分配：广东检测中心→黄浩浩，非广东→周涵林

**金额拆分与校验**：
- 金额 × 比例（保留完整精度，不四舍五入）
- `abs(拆分前 - 拆分后) < 1e-6` 视为通过

**输出**：原6列 + `销售` 列 = 7列，一行可拆为多行

### 3.6 Phase 4: 渲染看板

**入口**：`processors/run.py::run_render()`

| 页面 | 核心组件 |
|------|---------|
| 数据总览 | Hero 双环 + 事业部矩阵 + 销售 Top N |
| 年度达成 | Hero + 部门卡 + 客户达成矩阵(cell-bg) + 子公司抽屉弹窗 |
| 月度达成 | Hero + 部门卡 + 客户达成矩阵 + 子公司抽屉弹窗 |
| 季度达成 | Hero + 部门卡 + 客户达成矩阵 + 子公司抽屉弹窗 |
| 销售达成 | 卡片1:销售进度条 / 卡片2:销售×事业部矩阵 / 卡片3:销售×客户矩阵 + 抽屉弹窗 |
| 年度同比 | 事业部同比表 + 客户同比矩阵（无年基线粒度时降级） |

**客户矩阵展示顺序**（`_sorted_customers`）：
- **无「优先展示」配置**（如月度/季度页）→ 按**指标表原始顺序**展示聚合后的公司（`groupby`/`pivot_table` 均 `sort=False` 保留行序；指标表外但有实际的客户追加末尾）
- **有「优先展示」配置**（如年度页）→ 优先展示客户排前，其余按目标合计/实际金额降序
- 拆分母公司（科技公司）强制聚合按销售拆分，不因当月子公司数 <3 而退回裸母公司名

---

## 四、标准输出格式

### 4.1 收入/回款（6列标准表）

| 列名 | 类型 | 说明 |
|------|------|------|
| 事业部 | str | 检测/信息/能源/海外（已简称） |
| 金额 | float | 统一为**元** |
| 客户 | str | 白名单匹配后的标准客户名 |
| 法人主体 | str | 财务端来源保留；运营端取法人主体列 |
| 日期 | str/Timestamp | 清洗后统一 |
| 是否为广东公司 | str | "是" 或 空 |

### 4.2 销售收入/销售回款（7列）

6列 + `销售` 列（一行按比例可拆为多行）。

---

## 五、运行方式

```bash
# 一键运行（Windows，推荐）
run_all.bat

# 全流程
python main.py

# 分步
python main.py --step=yearly    # 仅年基线清洗
python main.py --step=clean     # 仅收入/回款清洗
python main.py --step=split     # 仅销售拆分
python main.py --step=render    # 仅渲染看板
python main.py --type=收入       # 仅收入链路
python main.py --from=clean --to=render   # 区间执行
python main.py --dry-run        # 预检（检查 raw/mappings/config）
python main.py --list           # 列出所有步骤
```

---

## 六、配置驱动

### 6.1 `cleaning_config.json`（清洗核心配置，8 段）

| 段 | 作用 |
|----|------|
| `_说明` | 配置整体说明 |
| `数据源` | 财务端（5来源）/运营端（2来源）Excel 路径 + 引擎 + 列映射 + 金额除数 |
| `年基线数据` | 往年收入/回款精确文件路径 |
| `客户名单` | 白名单 Excel 路径 |
| `映射文件` | 部门映射 + 客户名单 JSON 路径 |
| `时间范围` | 数据筛选区间（手动模式：改日期即生效） |
| `输出` | 清洗后 10 个输出目录 + 标准列 + 金额单位 |
| `手工指标表` | 6 张目标表路径 |

**时间范围**（手动模式，改日期重跑即生效）：
```json
"年度累计":   { "start_date": "2026-01-01",   "end_date": "2026-08-31 23:59:59" },
"月度数据":   { "start_date": "2026-08-01",   "end_date": "2026-08-31 23:59:59" },
"季度累计筛选": { "start_date": "2026-07-01",   "end_date": "2026-09-30 23:59:59" },
"年基线数据": { "年份": 2025, "月份范围": [1, 8] }
```
- 各段带 `_使用方` 字段标明影响页面

### 6.2 配置编辑器 `config/配置编辑器.xlsx`（编辑层）

| Sheet | 内容 | 规模 |
|-------|------|------|
| 时间配置 | 年度/月度/季度/年基线 时间范围 | 4 行 |
| 展示规则 | 6 页面的显示/最大行数/排序/优先展示/客户筛选 | ~50 行 |
| **KPI指标** | Hero 圆环指标总数覆盖（万元，留空=按指标文件合计） | 6 行 |
| 销售归属 | 母公司→子公司→指标→部门→销售→比例 | ~2340 行 |
| 说明 | 使用指南 | — |

**同步机制**：Excel 是"编辑层"，JSON 是"事实源"。`run_all.bat` 启动时自动运行 `scripts/config_excel_to_json.py` 同步；`python main.py` 不读 Excel。

---

## 七、映射规则

### 7.1 部门事业部映射（`data/mappings/部门事业部映射/`）

| 来源全称 | 简称 | 适用于 |
|----------|------|--------|
| 检测工程事业部 | 检测 | 收入+回款 |
| 信息智能事业部 | 信息 | 收入+回款 |
| 能源动力事业部 | 能源 | 收入+回款 |
| 海外事业部 | 海外 | 收入+回款 |

### 7.2 客户归拢策略

- **1:1 关系**（子公司名=母公司名）：不聚合，跳过映射
- **3+ 子公司** 或 **客户名=母公司名且出现在数据中**：聚合到母公司名
- **配置了销售拆分的母公司**（`_销售拆分.客户矩阵`）：即使当月数据中子公司数 <3 也强制聚合按销售拆分（如 科技公司 月度回款当月仅 2 家有数据，仍需拆成 王海龙/李巍）
- 归拢后子公司名不在列表中 → 弹窗数据从 `subs_with_data`（原始数据提取）读取
- `_consolidate_target` 对指标表两种口径均处理：按"子公司名+销售"（年度）或"客户=母公司+销售"（月度/季度）都能正确归并到 `母公司·销售`

### 7.3 内部交易排除

- 财务端：硬编码排除 3 家（广东汽车检测中心 / 中汽院深圳 / 中汽院湖南）
- 运营端：按 `是否属于内部交易` 列值过滤

---

## 八、测试套件

| 文件 | 说明 |
|------|------|
| test_config | 配置加载（含动态时间模式 11 用例） |
| test_column_resolver | 列名冗余解析 |
| test_customer_matcher | 白名单匹配 |
| test_mapping_loader | 部门映射加载 |
| test_display_rules | 展示规则读取 |
| test_mock_page_data | 页面数据 Mock（23 用例） |
| test_utils | 清洗工具函数 |
| test_yearly_baseline | 年基线清洗 |
| test_render_utils | 格式化（fmt_wan/fmt_pct/fmt_yoy） |
| test_main | 调度器 |
| test_page_renderers / test_render | 页面渲染（需数据） |
| test_e2e / test_data_loader | 端到端（需数据） |
| test_mock_page_data | Mock 数据层（23 用例，不依赖真实 Excel） |

**当前**：**147 passed, 0 skipped**（数据齐全时全部真实运行；`tests/conftest.py` 统一数据就绪检查）
